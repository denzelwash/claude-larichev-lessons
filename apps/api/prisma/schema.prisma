generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added in the next iteration:
// - User (placeholder, until auth is introduced)
// - Category
// - Expense
